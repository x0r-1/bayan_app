name: Athar Database Merger
on:
  schedule:
    - cron: '*/5 * * * *' 
  workflow_dispatch: 

permissions:
  contents: write

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
      - name: Run Merge Script
        run: python merge_queue.py
      - name: Save Main Database
        run: |
          git config --global user.name 'Athar Merger'
          git config --global user.email 'bot@athar.com'
          # التعديل هنا: بنضيف videos.json وبنأكد حذف مجلد queue
          git add videos.json queue/ 
          git commit -m "Periodic Merge: Updated videos.json and likes" || exit 0
          git push
